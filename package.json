{
  "name": "cleanthis-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for the CleanThis browser extension (the extension itself is plain JavaScript — no bundler).",
  "scripts": {
    "build": "node build.js",
    "lint:firefox": "web-ext lint --source-dir dist/firefox"
  },
  "devDependencies": {
    "web-ext": "^8.7.1"
  }
}
